using UnityEngine;

public class SpawnPos
{
    public Vector3 position;
    public Vector3 forward;

    public SpawnPos(Vector3 position, Vector3 forward)
    {
        this.position = position;
        this.forward = forward;
    }
}
